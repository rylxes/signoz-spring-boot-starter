<?xml version="1.0" encoding="UTF-8"?>
<!--
  SigNoz base Logback configuration (Spring Boot 3.x).

  Include in your logback-spring.xml:
    <include resource="logback-signoz.xml"/>
-->
<included>

    <appender name="SIGNOZ_JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="io.signoz.springboot.logging.SigNozJsonEncoder">
            <!-- Excludes Spring Boot's bootstrap LoggerContext properties (CONSOLE_LOG_PATTERN,
                 FILE_LOG_PATTERN, PID, ...) from every event. Flip to true only if you
                 intentionally publish custom context properties worth emitting. -->
            <includeContext>false</includeContext>
            <includeMdc>true</includeMdc>
            <includeStructuredArguments>true</includeStructuredArguments>
            <fieldNames>
                <timestamp>@timestamp</timestamp>
                <message>message</message>
                <logger>logger</logger>
                <thread>thread</thread>
                <level>level</level>
                <levelValue>[ignore]</levelValue>
            </fieldNames>
        </encoder>
    </appender>

    <appender name="SIGNOZ_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} [traceId=%X{traceId} spanId=%X{spanId}] - %msg%n</pattern>
        </encoder>
    </appender>

    <springProfile name="local,dev">
        <root level="INFO">
            <appender-ref ref="SIGNOZ_CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="staging,production,prod">
        <root level="INFO">
            <appender-ref ref="SIGNOZ_JSON"/>
        </root>
    </springProfile>

</included>
